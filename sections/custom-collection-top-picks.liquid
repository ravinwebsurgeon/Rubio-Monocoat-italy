{% comment %} {% assign images = collection.metafields.custom.top_picks_images.value %}
  {% assign titles = collection.metafields.custom.top_picks_titles.value %} {% endcomment %}

  {% assign descriptions = collection.metafields.custom.top_picks_desc.value %}
  {% assign product_list = collection.metafields.custom.top_picks_product.value %}
  {% assign heading = collection.metafields.custom.top_picks_heading.value %}

  {% if product_list != blank %}
<section class="top_picks_slider custom_categrysss">
  <div class="top-picks-header">
    <h2>
      {{ heading }}
    </h2>    
  </div>
  <div style="position:relative;">    
    <div class="top-silder-arrows">
      <div class="prev-item-btn">
        <button type="button" class="cert-prev">
          <svg xmlns="http://www.w3.org/2000/svg" viewBox="0 0 640 640">
            <path d="M169.4 297.4C156.9 309.9 156.9 330.2 169.4 342.7L361.4 534.7C373.9 547.2 394.2 547.2 406.7 534.7C419.2 522.2 419.2 501.9 406.7 489.4L237.3 320L406.6 150.6C419.1 138.1 419.1 117.8 406.6 105.3C394.1 92.8 373.8 92.8 361.3 105.3L169.3 297.3z"/>
          </svg>
        </button>
      </div>
      <div class="next-item-btn">
        <button type="button" class="cert-next">
          <svg xmlns="http://www.w3.org/2000/svg" viewBox="0 0 640 640">
            <path d="M471.1 297.4C483.6 309.9 483.6 330.2 471.1 342.7L279.1 534.7C266.6 547.2 246.3 547.2 233.8 534.7C221.3 522.2 221.3 501.9 233.8 489.4L403.2 320L233.9 150.6C221.4 138.1 221.4 117.8 233.9 105.3C246.4 92.8 266.7 92.8 279.2 105.3L471.2 297.3z"/>
          </svg>
        </button>
      </div>
    </div>
    <div class="top_picks_carousel">
    {% for product in product_list %}
      {% assign i = forloop.index0 %}
      <div class="top-picks-product-card">
         {% if product.featured_image %}
            <div class="slider-product-image">
              <a href="{{ product.url }}">
                {% if product.metafields.custom.cover_image != blank %}
                  <img class="cover-image" src="{{ product.metafields.custom.cover_image | img_url: 'master' }}" alt="{{ product.title }}" width="" height="">
                {% endif %}
                <img class="featured-image" src="{{ product.featured_image | img_url: 'master' }}" width="" height="" />
                </a>
            </div>
          {% endif %}

        <div class="top-product-content">
          <h3>{{ product.title }}</h3>
          <p>{{ product.metafields.custom.collectionproduct_subtitle.value }}</p>
          <div class="ctificte-btn--pulsss">
            <div class="certificate-puls-btn">
              <button class="cert-plus" type="button" data-product-index="{{ i }}">+</button>
            </div>
            <div class="cart_btn_main">
              <button class="custom_product_cart_btn" data-variant-id>Add to Cart</button>
            </div>
          </div>
        </div>
      </div>
    {% endfor %}
  </div>
  </div>
  
  <!-- Product Detail Popup -->
  <div id="product-detail-popup" class="product-popup">
    <div class="popup-overlay"></div>
    <div class="popup-content">
      <button class="popup-close">&times;</button>
      <div class="popup-body">
        <div class="popup-image">
          <img id="popup-product-image" src="" alt="" />
        </div>
        <div class="popup-details">
          <h3 id="popup-product-title"></h3>
          <p id="popup-product-subtitle"></p>
          <div id="popup-product-description"></div>
          <div class="popup-actions">
            <button class="btn-view-product">View Product</button>
            <button class="custom_product_cart_btn ajax_Add_to_cart" data-variant-id>Add to Cart</button>
          </div>
        </div>
      </div>
    </div>
  </div>
  
</section>
{% endif %}

<style>
.custom_product_cart_btn{display:none;}
{% comment %}hover effect on images{% endcomment %}
.slider-product-image {
  position: relative;
  overflow: hidden;
}

.slider-product-image .featured-image,
.slider-product-image .cover-image {
  width: 100%;
  height: 100%;
  object-fit: cover;
  transition: opacity 0.4s ease, transform 0.4s ease;
  position: absolute;
  top: 0;
  left: 0;
}

/* Cover image visible by default */
.slider-product-image .cover-image {
  opacity: 1;
  z-index: 1;
}

/* Featured image hidden by default */
.slider-product-image .featured-image {
  opacity: 0;
  z-index: 2;
}

/* Hover effect: Show featured image, hide cover image */
.top-picks-product-card:hover .featured-image {
  opacity: 1;
}

.top-picks-product-card:hover .cover-image {
  opacity: 0;
}

/* Optional: Add scale effect for smooth transition */
.top-picks-product-card:hover .featured-image,
.top-picks-product-card:hover .cover-image {
  transform: scale(1.02);
}
{% comment %}hover effect on images end{% endcomment %}

  .top_picks_slider .top-product-content {
    border: 1px solid #183029;
    border-top: 0;
    border-bottom-left-radius: 20px;
    border-bottom-right-radius: 20px;
    padding: 15px;
    gap: 10px;
    justify-content: space-between;
    align-items: center;
    display:flex;
    background-color:#183029;
    position: relative;
    height:100%;
  }
.top_picks_slider .ctificte-btn--pulsss {
    justify-content: end;
    display: flex;
    margin-top: 0;
    position: absolute;
    left: 0;
    right: 0;
    top: 0;
    bottom: 0;
    align-items: center;
    padding: 0 10px;
}
  .top_picks_slider {
    padding: 100px 30px;
    max-width: 1745px;
    margin: 0 auto;
    position: relative;
  }
  .top-picks-header {
    display: flex;
    justify-content: space-between;
    align-items: center;
    margin-bottom: 50px;
  }
  .top-picks-header h2 {
    font-family: Frutiger Neue LT W1G;
    font-weight: 700;
    font-size: 40px;
    line-height: 45px;
    margin: 0;
  }
  .top_picks_slider .top-silder-arrows button {
    background: transparent;
    border: 0;
    padding: 0;
  }
  .top_picks_slider .top-silder-arrows button svg {
    width: 20px;
    height: 20px;
    display: block;
    cursor: pointer;
  }
  .top_picks_slider.custom_categrysss .top-silder-arrows .prev-item-btn, 
  .top_picks_slider.custom_categrysss .top-silder-arrows .next-item-btn {
    width: 40px;
    height: 40px;
    border: 1px solid #000;
    border-radius: 50%;
    display: flex;
    align-items: center;
    justify-content: center;
    cursor: pointer;
    position: absolute;
    top: 50%;
    transform: translateY(-50%);
    z-index:2;
  }
  .top_picks_slider .top-silder-arrows {
    display: flex;
    gap: 10px;
    cursor: pointer;
    transition:all ease 0.3s;
  }
 .top_picks_slider .top-silder-arrows .prev-item-btn {
    left: -20px;
}
section.top_picks_slider.custom_categrysss .top-picks-header {
    text-align: center;
    justify-content: center;
}

   .top_picks_slider .top-picks-product-card {
    border-radius: 20px;
    text-align: center;
    margin: 0 10px;
    overflow: hidden;
    position: relative;
    display: flex;
    flex-direction: column;
  }
  section.top_picks_slider.custom_categrysss .top-product-content{
  text-align:left;
  display: flex;
  background-color:transparent;
  border-color:transparent;
  flex-wrap: wrap;
}
section.top_picks_slider.custom_categrysss .top-product-content h3,section.top_picks_slider.custom_categrysss .certificate-puls-btn button.cert-plus{
    color: #000;
}
section.top_picks_slider.custom_categrysss .top-picks-product-card {
    border: 1px solid #ccc;
}
section.top_picks_slider.custom_categrysss .certificate-puls-btn{
  border-color:#545454;
}
section.top_picks_slider.custom_categrysss .top-product-content p {
    margin: 0;
    color: #000;
    font-size: 14px;
    width: calc(100% - 28px);
}
section.top_picks_slider.custom_categrysss {
    max-width: 1700px;
    width: 95%;
    padding-bottom:30px;
}
.top_picks_slider.custom_categrysss .top-silder-arrows .next-item-btn {
    right: -20px;
}
.custom_categrysss .top_picks_carousel {
    padding: 0 20px;
}
  .top-picks-product-card .slider-product-image {
    background-color: #E1E9E4;
    position: relative;
    padding-bottom: 240px;
    border-top-left-radius: 20px;
    border-top-right-radius: 20px;
    overflow: hidden;
  }
section.top_picks_slider.custom_categrysss .slider-product-image{
  background-color:transparent;
}
  .top-picks-product-card img {
    max-width: 100%;
    width: 100%;
    height: 100%;
    margin: 0 auto;
    display: block;
    position: absolute;
    top: 0; bottom: 0; left: 0; right: 0;
    object-fit: cover;
    transition: opacity 0.3s ease;
  }
  .top-picks-product-card h3 {
    margin: 0;
    font-family: Frutiger Neue LT W1G;
    font-weight: 500;
    font-size: 18px;
    line-height: normal;
    text-align: left;
    color:#fff;
    padding-right: 36px;
    width: calc(100% - 28px);
  }
  .top_picks_slider .certificate-puls-btn button.cert-plus {
    background: 0;
    border: 0;
    font-size: 15px;
    padding: 0;
    cursor: pointer;
    color:#545454 !important;
  }
  .top_picks_slider .certificate-puls-btn {
    width: 24px;
    height: 24px;
    border: 1px solid #fff;
    border-radius: 50%;
    display: flex;
    align-items: center;
    justify-content: center;
    cursor: pointer;
  }

  /* Product Detail Popup Styles */
  .product-popup {
    display: none;
    position: fixed;
    top: 0;
    left: 0;
    width: 100%;
    height: 100%;
    z-index: 1000;
    align-items:center;
  }
  .popup-image img {
    width: 100%;
    padding: 12px;
    height: 100%;
    display: block;
    object-fit: contain;
    border-radius: 0;
    aspect-ratio: 1;
}
  .popup-overlay {
    position: absolute;
    top: 0;
    left: 0;
    width: 100%;
    height: 100%;
    background-color: rgba(0, 0, 0, 0.7);
  }
  
  .popup-content {
    position: relative;
    background: white;
    max-width: 640px;
    width: 95%;
    max-height: 80vh;
    margin: 10vh auto;
    border-radius: 20px;
    display: flex;
    flex-direction: column;
  }
  
 .popup-body {
    display: flex;
    height: 100%;
    overflow: auto;
  }
  
  .popup-image {
    width: 55%;
    background-color: #f5f5f5;
    border-radius: 12px 0 0 12px;
  }
  
  .popup-details {
    padding: 20px 25px;
    display: flex;
    flex-direction: column;
    width: 45%;
    align-self: center;
  }
  .popup-details div#popup-product-description p {
    margin: 0;
}

.popup-actions button {
    background-color: #0f2d1c;
    border-radius: 50px;
    color: #fff;
    cursor: pointer;
    outline: none;
    border: none;
    padding: 0;
    font-size: 13px;
    font-family: Frutiger Neue LT W1G;
    font-weight: 500;
    letter-spacing: normal;
    line-height: normal;
    height: 40px;
    min-height: inherit;
    width: 100%;
    min-width: inherit;
}
  .popup-details h3 {
    margin: 10px 0;
    font-weight: 600;
    letter-spacing: normal;
    line-height: normal;
    font-family: Frutiger Neue LT W1G;
  }
  
  .popup-details p {
    color: #666;
    margin: 0 0 5px;
  }
  
  .popup-description {
    margin: 20px 0;
    flex-grow: 1;
  }
  
  .popup-actions {
    margin-top: 20px;
    display:flex;
    flex-wrap:wrap;
    gap:10px;
  }
  .custom-popup-overlay:after {
    background: rgba(0, 0, 0, 0.5);
    position: fixed;
    left: 0;
    right: 0;
    top: 0;
    bottom: 0;
    content: "";
    z-index: 11;
}

button.popup-close {
    position: absolute;
    top: -5px !important;
    right: -5px !important;
    cursor: pointer;
    z-index: 2;
    line-height: 24px;
    border: none;
    outline: none;
    display: flex;
    justify-content: center;
    align-items: center;
    height: 25px;
    width: 25px;
    border-radius: 50%;
    background-color: #183029;
    color: #fff;
}
  

  /* Active state for plus button */
  .cert-plus.active {
    transform: rotate(45deg);
  }

  .top_picks_slider .slick-list {
    display: flex;
  }

  .top_picks_slider .slick-list .slick-track {
    display: flex;
  }

  @media(max-width:1900px){
    .top_picks_slider {
      padding: 50px 30px;
    }
    .top-picks-header h2 {
      font-size: 28px;
      line-height: normal;
    }
    .top-picks-header {
      margin-bottom: 30px;
    }
  }
  
  @media(max-width:991px){
    .top_picks_slider {
        padding: 50px 20px;
    }
    .top_picks_slider .top-picks-product-card .slider-product-image {
        padding-bottom: 190px;
    }
  }
  
  @media(max-width:767px){
    .top-picks-header h2 {
        font-size: 24px;
    }
    .top_picks_slider {
        padding: 30px 20px;
        position: relative;
    }
    .top_picks_slider .top-silder-arrows {
        position: absolute;
        z-index: 1;
        top: 53%;
        transform: translateY(-40%);
        left: 18px;
        right: 18px;
        display:none;
    }
    .custom_categrysss .top_picks_carousel {
        padding: 0;
    }
    section.top_picks_slider.custom_categrysss {
        width: 100%;
    }
    .top_picks_slider .top-silder-arrows button svg {
      width: 12px;
      height: 12px;
    }
    .top_picks_slider .top-silder-arrows .prev-item-btn,  .top_picks_slider .top-silder-arrows .next-item-btn {
      width: 24px;
      height: 24px;
      border: 1px solid rgba(0, 0, 0, 0.2);
    }
    .top_picks_slider  .top-silder-arrows .next-item-btn{
      margin-left:auto;
    }
   
    .top_picks_slider .certificate-puls-btn {
      width: 24px;
      height: 24px;
    }
    .top_picks_slider .certificate-puls-btn button.cert-plus {
      font-size: 18px;
    }
    
    .popup-content {
      width: 95%;
      margin: 5vh auto;
    }
    
    .popup-details {
        padding: 20px;
        width: 100%;
    }
    .popup-image {
      width: 100%;
      background-color: #f5f5f5;
      border-radius: 12px 0 0 12px;
    }
    .popup-body {
      flex-wrap: wrap;
    }    
    .popup-details h3 {
      font-size: 20px;
      margin-top: 0;
    }
    .popup-actions button {
        flex: 1;
    }
  }
</style>
<script>
document.addEventListener("DOMContentLoaded", function() {
//add product into cart
document.querySelectorAll('.custom_product_cart_btn').forEach(function (button) {
        button.addEventListener('click', function () {
            const variantId = button.dataset.variantId;
            fetch('/cart/add.js', {
                method: 'POST',
                headers: {
                    'Content-Type': 'application/json',
                    'Accept': 'application/json'
                },
                body: JSON.stringify({
                    id: variantId,
                    quantity: 1
                })
            })
            .then(response => response.json())
            .then(data => {
                console.log('Added to cart:', data);
                updateCartCount(1);
                refreshCartDrawer();
            })
            .catch(error => {
                console.error('Error adding to cart:', error);
                alert("Something went wrong.");
            });
        });
    });

function updateCartCount(totalQuantityAdded) {
    const cartCounts = document.querySelectorAll('.cart-count-bubble > span');
    const customCount = document.querySelector('.custom-cart-count-bubble > span');

    if (cartCounts.length > 0) {
        // Update all cart bubbles (standard or custom)
        cartCounts.forEach(count => {
            const currentNumber = Number(count.textContent) || 0;
            const newNumber = currentNumber + totalQuantityAdded;
            count.textContent = newNumber;
            count.parentNode.style.display = 'block';
            count.parentNode.style.visibility = 'visible';
        });
    } else if (customCount) {
        // Update existing custom bubble
        let number = Number(customCount.textContent) || 0;
        customCount.textContent = number + totalQuantityAdded;
        customCount.parentNode.style.display = 'block';
        customCount.parentNode.style.visibility = 'visible';
    } else {
        // Create new bubble if none exists
        const mainCartBubble = document.querySelector('.header__icon--cart');
        if (mainCartBubble) {
            const newCount = document.createElement('div');
            newCount.classList.add('custom-cart-count-bubble','cart-count-bubble');

            const newCountSpan = document.createElement('span');
            newCountSpan.textContent = totalQuantityAdded;

            newCount.appendChild(newCountSpan);
            mainCartBubble.appendChild(newCount);
            newCount.style.display = "block";
            newCount.style.visibility = "visible";
        }
    }
}

function refreshCartDrawer() {
  const cartDrawer = document.querySelector('cart-drawer');

    fetch(`${routes.cart_url}?sections=cart-drawer`)
      .then(response => response.json())
      .then(sections => {
        const newCartDrawerHtml = sections['cart-drawer'];
        const newDocument = new DOMParser().parseFromString(newCartDrawerHtml, 'text/html');
        const newCartDrawer = newDocument.querySelector('cart-drawer');

        if (cartDrawer && newCartDrawer) {
          cartDrawer.replaceWith(newCartDrawer);
          document.documentElement.dispatchEvent(new CustomEvent('cart:refresh', { bubbles: true }));
          newCartDrawer.classList.add('active', 'animate');
        }
      })
      .catch((error) => { console.error('Error refreshing cart drawer:', error); });
  }



  // Store product data for popup
  const productData = [
    {% for product in product_list %}
      {% assign i = forloop.index0 %}
      {
        image: "{{ product.featured_image | img_url: 'master' }}",
        title: "{{ product.title | escape }}",
        subtitle: "{{ product.metafields.custom.collectionproduct_subtitle.value | escape }}",
        description: "{{ product.description }}",
        variantId: "{{ product.variants.first.id }}",
        inventory_quantity: {{ product.variants.first.inventory_quantity | default: 0 }},
        url: "{{ product.url }}"
      }{% unless forloop.last %},{% endunless %}
    {% endfor %}
  ];

  // Init Slick
  $('.top_picks_carousel').slick({
    slidesToShow: 5,
    slidesToScroll: 1,
    infinite: false,
    prevArrow: $('.cert-prev'),
    nextArrow: $('.cert-next'),
    responsive: [
      {
        breakpoint: 1200,
        settings: {
          slidesToShow: 3,
          slidesToScroll: 1
        }
      },
      {
        breakpoint: 767,
        settings: {
          slidesToShow: 1,
          dots: true,
          arrows: false
        }
      }
    ]
  });

let btn_elements = document.querySelectorAll('.ctificte-btn--pulsss');

btn_elements.forEach(function(element) {
    const parent = element.closest('.custom_product_cart_btn')?.parentElement;
    
    if (parent) {
      console.log(parent,'found')
        parent.addEventListener('mouseenter', function() {
            element.style.display = 'none';
            element.closest('.custom_product_cart_btn').style.display = 'block';
        });
        
        parent.addEventListener('mouseleave', function() {
            element.style.display = 'flex';
            element.closest('.custom_product_cart_btn').style.display = 'none';
        });
    }
});


  const cartBtn = document.querySelector('.ajax_Add_to_cart');
  let qty = product.inventory_quantity
  console.log(productData,'check qty')
  if(qty == 0){
      cartBtn.classList.add('disabled-btn')
      cartBtn.disabled = true;
  }
  cartBtn.setAttribute('data-variant-id',variantId)


  {% comment %} // Popup functionality
  const popup = document.getElementById('product-detail-popup');
  const popupImage = document.getElementById('popup-product-image');
  const popupTitle = document.getElementById('popup-product-title');
  const popupSubtitle = document.getElementById('popup-product-subtitle');
  const popupDescription = document.getElementById('popup-product-description');
  const viewProductBtn = document.querySelector('.btn-view-product');
  const cartBtn = document.querySelector('.ajax_Add_to_cart');
  let currentProductIndex = 0;
  let currentProductUrl = '';

  // Open popup when plus button is clicked
  document.querySelectorAll(".cert-plus").forEach(btn => {
    btn.addEventListener("click", function() {
      const productIndex = parseInt(this.getAttribute('data-product-index'));
      currentProductIndex = productIndex;
      
      // Update button to show minus
      this.textContent = "x";
      this.classList.add('active');
      // Show popup with product details
      showProductPopup(productIndex);
    });
  });
  // Close popup
  document.querySelector('.popup-close').addEventListener('click', closePopup);
  document.querySelector('.popup-overlay').addEventListener('click', closePopup);

  // View product button
  viewProductBtn.addEventListener('click', function() {
    window.location.href = currentProductUrl;
  });

  function showProductPopup(index) {
    const product = productData[index];
    
    popupImage.src = product.image;
    popupImage.alt = product.title;
    popupTitle.textContent = product.title;
    popupSubtitle.textContent = product.subtitle;
    popupDescription.innerHTML  = product.description;
    currentProductUrl = product.url;
    variantId= product.variantId
    let qty = product.inventory_quantity
    console.log(productData,'check qty')
    if(qty == 0){
      cartBtn.classList.add('disabled-btn')
      cartBtn.disabled = true;
    }
    cartBtn.setAttribute('data-variant-id',variantId)
    popup.style.display = 'flex';
    document.body.style.overflow = 'hidden';
    document.body.classList.add('custom-popup-overlay')
  }

  function closePopup() {
    popup.style.display = 'none';
    document.body.style.overflow = 'auto';
        document.body.classList.remove('custom-popup-overlay')

    // Reset all plus buttons
    document.querySelectorAll(".cert-plus").forEach(btn => {
      btn.textContent = "+";
      btn.classList.remove('active');
    });
  }

  // Close popup with Escape key
  document.addEventListener('keydown', function(e) {
    if (e.key === 'Escape') {
      closePopup();
    }
  }); {% endcomment %}
});


</script>

{% schema %}
{
  "name": "Cat Top Picks Slider",
  "presets": [
    {
      "name": "Cat Top Picks Slider"
    }
  ]
}
{% endschema %}