{% stylesheet %}
.blog-slider-section {
  padding:50px 0;
  font-family: Frutiger Neue LT W1G;
}
.blog-slider-section .container{
  max-width: 1760px;
  padding: 0 30px;
}
.blog-slider.slick-slider {
    overflow: hidden;
}
.blog-slider-header {
  display: flex;
  justify-content: space-between;
  align-items: center;
  margin-bottom: 50px;
}
.blog-slider-section .blog-slider-header h2 {
  font-family: Frutiger Neue LT W1G;
  font-weight: 700;
  font-size: 36px;
  line-height: normal;
  margin: 0;
}
.blog-slider.slick-slider .slick-list{
  margin:0 -25px;
  display: flex;
}
.blog-slider-arrows {
  display: flex;
  gap: 10px;
}
.blog-slider-arrows button {
  background: transparent;
  border: 1px solid #183029;
  border-radius: 50%;
  width: 40px;
  height: 40px;
  display: flex;
  align-items: center;
  justify-content: center;
  cursor: pointer;
}
.blog-slider-arrows button svg {
  width: 18px;
  height: 18px;
  color: #183029;
}
.blog-slider-section .slick-track{
   display: flex;
}
.blog-card.slick-slide {
  background: #fff;
  border-radius: 20px;
  overflow: hidden;
  margin:0 25px;
  height: 100%;
  display: flex;
  border: 1px solid rgb(0 0 0 / 30%);
  box-sizing: border-box;
}
.blog-card-image {
  width: 100%;
  height: 400px;
  overflow: hidden;
  object-fit: cover;
}
.blog-card-image img {
  width: 100%;
  height: 100%;
  object-fit: cover;
  display: block;
   border-radius: 20px 20px 0 0;
   transition: all ease 0.3s;
}
.blog-card-content {
  padding: 30px;
  display: flex;
  justify-content: space-between;
  align-items: flex-end;
}
.blog-card-content h3 {
  margin: 0 0 28px;
  flex: 1;
  font-family: Frutiger Neue LT W1G;
  font-weight: 500;
  font-size: 28px;
  line-height: normal;
  letter-spacing: normal;
}
.blog-card-arrow {
  width: 40px;
  height: 40px;
  border-radius: 50%;
  border: 1px solid #183029;
  display: flex;
  align-items: center;
  justify-content: center;
  margin-left: 15px;
}
.blog-card-arrow svg {
  width: 18px;
  height: 18px;
  color: #183029;
}
.blog-card-arrow svg path{
  stroke: #000;
}
.blog-card-content h3 a {
    color: #000;
    text-decoration: none;
}
.blog-card-image a:hover img {
    transform: scale(1.1);
    transition: all ease 0.3s;
}
@media(max-width: 1024px){
  .blog-card { width: 100%; height: auto; }
  .blog-card-image { height: 300px; }
}
{% endstylesheet %}

<section class="blog-slider-section">
  <div class="container"> 
    <div class="blog-slider-header">
      <h2>{{ section.settings.heading }}</h2>
      <div class="blog-slider-arrows"></div>
    </div>
    <div class="blog-slider">
      {% for article in blogs[section.settings.blog].articles limit: section.settings.post_limit %}
        <div class="blog-card">
          <div class="blog-card-image">
            {% if article.image %}
              <a href="{{ article.url }}">
                <img src="{{ article.image | img_url: 'master' }}" alt="{{ article.title }}" width="" height="">
              </a>
            {% endif %}
          </div>
          <div class="blog-card-content">
            <h3>
              <a href="{{ article.url }}">
                {{ article.title }}
              </a>
            </h3>
            <a href="{{ article.url }}" class="blog-card-arrow">
              <svg xmlns="http://www.w3.org/2000/svg" fill="none" viewBox="0 0 24 24" stroke="currentColor">
                <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M9 5l7 7-7 7" />
              </svg>
            </a>
          </div>
        </div>
      {% endfor %}
    </div>
  </div>  
</section>

{% javascript %}
document.addEventListener("DOMContentLoaded", function() {
  if ($('.blog-slider').length) {
    $('.blog-slider').slick({
      slidesToShow: 2,
      slidesToScroll: 1,
      infinite: false,
      arrows: true,
      appendArrows: $('.blog-slider-arrows'),
      prevArrow: `<button type="button" class="slick-prev">
        <svg xmlns="http://www.w3.org/2000/svg" fill="none" viewBox="0 0 24 24" stroke="currentColor">
          <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M15 19l-7-7 7-7" />
        </svg>
      </button>`,
      nextArrow: `<button type="button" class="slick-next">
        <svg xmlns="http://www.w3.org/2000/svg" fill="none" viewBox="0 0 24 24" stroke="currentColor">
          <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M9 5l7 7-7 7" />
        </svg>
      </button>`,
      responsive: [
        { breakpoint: 1024, settings: { slidesToShow: 1 } }
      ]
    });
  }
});
{% endjavascript %}

{% schema %}
{
  "name": "Blog Slider",
  "settings": [
    { "type": "text", "id": "heading", "label": "Section Heading", "default": "Latest from the blog" },
    { "type": "blog", "id": "blog", "label": "Select Blog" },
    { "type": "range", "id": "post_limit", "label": "Number of posts", "min": 2, "max": 12, "step": 1, "default": 6 }
  ],
  "presets": [
    { "name": "Blog Slider", "category": "Blog" }
  ]
}
{% endschema %}
