{% comment %}
  Renders a swatch component.
  Accepts:
  - swatch: {Object} a swatch object
  - shape: {String} swatch shape. Accepts 'square', defaults to circle.

  Usage:
  {% render 'swatch',
    swatch: value.swatch
    shape: 'square'
  %}
{% endcomment %}

{%- liquid
  assign swatch_value = null
  if swatch.image
    assign image_url = swatch.image | image_url: width: 50
    assign swatch_value = 'url(' | append: image_url | append: ')'
    assign swatch_focal_point = swatch.image.presentation.focal_point
  elsif swatch.color
    assign swatch_value = 'rgb(' | append: swatch.color.rgb | append: ')'
  endif
-%}

<span
  {% if swatch_value %}
    class="swatch{% if shape == 'square' %} swatch--square{% endif %}"
    style="--swatch--background: {{ swatch_value }};{% if swatch_focal_point %} --swatch-focal-point: {{ swatch_focal_point }};{% endif %}"
  {% else %}
    class="swatch swatch--unavailable{% if shape == 'square' %} swatch--square{% endif %}"
  {% endif %}
></span>


{% comment %} {% for variant in product.variants %}
  <div class="variant-image">
    {% if variant.metafields.custom.variant_image != blank %}
      <img 
        src="{{ variant.metafields.custom.variant_image | img_url: 'master' }}" 
        alt="{{ variant.title }}" 
        width="100%" 
        height="100%">
    {% endif %}
  </div>
{% endfor %} {% endcomment %}


{% comment %} <div class="meta-pr">
  {% if product.selected_or_first_available_variant.metafields.custom.variant_image != blank %}
    <img 
      src="{{ product.selected_or_first_available_variant.metafields.custom.variant_image | image_url: width: 200 }}" 
      alt="{{ product.selected_or_first_available_variant.title }}">
  {% endif %}
</div> {% endcomment %}

 {% comment %} <div class="meta-pr">
  {% for variant in product.variants %}
    {% if variant.metafields.custom.variant_image != blank %}
      <div class="variant-image">
        <img 
          src="{{ variant.metafields.custom.variant_image | image_url: width: 200 }}" 
          alt="{{ variant.title }}">
      </div>
    {% endif %}
  {% endfor %}
</div>  {% endcomment %}
{%- if product.selected_or_first_available_variant.metafields.custom.variant_image != blank -%}
  <img 
    src="{{ product.selected_or_first_available_variant.metafields.custom.variant_image | image_url: width: 800 }}" 
    alt="{{ product.selected_or_first_available_variant.title }}" 
    class="variant-metafield-image"
  >
{%- endif -%}


